const prompt = require("prompt-sync")();

/*et tableau = [2,1,6,8,9,3,7]
 let max = tableau[0]
 let min = tableau [0]
for (i=1;i<tableau.length;i++){
    if(tableau[i]>max){
        max=tableau[i]}
    if (tableau [i]<min){
        min=tableau[i]
    }

}
console.log("le plus grand est" +max);
console.log("le plus petite est" +min);*/




/*let tableau = [1,2,35,4,5,6,74,8,91,10];
let somme = 0;

for(let i=0; i<tableau.length; i++){
    somme =somme + tableau[i];
}
moyenne=somme / tableau.length;


console.log("la somme est :" + somme);
console.log ("la moyenne est :" + moyenne); */



let tableau = [2,8,7,3];
for(let i=0; i<tableau.length; i++){
      carré = tableau[i] * tableau[i]
}
console.log("carré de tableau est " +carré);